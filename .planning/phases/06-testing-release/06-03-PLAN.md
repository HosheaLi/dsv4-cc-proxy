---
phase: 06-testing-release
plan: 03
type: execute
security_review: true
wave: 1
depends_on: []
files_modified:
  - README.md
  - README.zh-CN.md
  - CHANGELOG.md
  - docs/dev/codex-integration.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-14, D-15, D-16, D-17]
user_setup: []

must_haves:
  truths:
    - "README.md + README.zh-CN.md 包含 Codex Support 章节，描述三个环境变量和两个端点"
    - "CHANGELOG.md 包含 [2.0.0] 条目，按模块归类 Added/Changed"
    - "docs/dev/codex-integration.md 存在，包含架构概览、翻译流程、SSE 生命周期事件序列"
    - "CI 中 publish job 使用 PyPI OIDC Trusted Publishing"
    - "CI test job 遍历所有测试 `pytest tests/ -v` 并生成覆盖率 badge"
    - "CI 简化到单 Python 版本（3.12）"
    - "CI Docker 标签包含 sha 标签"
  artifacts:
    - path: "README.md"
      provides: "Codex Support 章节 + coverage badge 引用"
      contains: "## Codex Support"
    - path: "README.zh-CN.md"
      provides: "中文版 Codex Support 章节"
      contains: "## Codex 支持"
    - path: "CHANGELOG.md"
      provides: "[2.0.0] 发布条目"
      contains: "## [2.0.0]"
    - path: "docs/dev/codex-integration.md"
      provides: "Codex 集成技术文档"
      min_lines: 80
    - path: ".github/workflows/ci.yml"
      provides: "PyPI OIDC publish job、覆盖率 badge、单版本、Docker sha 标签"
      contains: "id-token: write"
  key_links:
    - from: "README.md Codex Support"
      to: "dsv4_cc_proxy/codex/ 模块"
      via: "文档描述的环境变量和端点"
    - from: "docs/dev/codex-integration.md"
      to: "docs/dev/deepseek-thinking-proxy.md"
      via: "并列的技术设计文档"
    - from: ".github/workflows/ci.yml publish job"
      to: "PyPI OIDC"
      via: "pypa/gh-action-pypi-publish@release/v1 + id-token: write"
---

<objective>
**文档更新 & CI 改进**

Purpose: 完成所有用户文档（README 英文 + 中文、CHANGELOG、技术文档）和 CI 配置（PyPI OIDC、覆盖率 badge、单版本测试、Docker sha 标签）。
Output: 4 个文档文件更新/创建、1 个 CI 配置文件更新
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-release/06-CONTEXT.md
@.planning/phases/06-testing-release/06-RESEARCH.md
@.planning/phases/06-testing-release/06-PATTERNS.md

参考文件:
- README.md (现有英文 README, 在其上追加)
- README.zh-CN.md (现有中文 README, 同步追加)
- CHANGELOG.md (现有 changelog 格式参考 [1.8.0] 条目)
- docs/dev/deepseek-thinking-proxy.md (codex-integration.md 的格式模板)
- .github/workflows/ci.yml (现有 CI, 追加 PyPI job 和标签)
</context>

<tasks>

<task type="auto">
<name>Task 1: Update README.md + README.zh-CN.md — Codex Support 章节</name>
<files>README.md, README.zh-CN.md</files>

<read_first>
- README.md (完整阅读了解现有结构与风格)
- README.zh-CN.md (完整阅读, 了解中文术语和格式)
</read_first>

<action>
在 README.md 和 README.zh-CN.md 中新增 Codex Support 章节。

**英文 README.md 修改位置:** 在 "## Why dsv4-cc-proxy" 章节后、"## Quick Start" 前插入新的 "## Codex Support" 章节。

**需要包含的内容（参照 PATTERNS.md 结构 + CONTEXT.md D-07 要求）:**

```markdown
## Codex Support

dsv4-cc-proxy also translates between the [OpenAI Responses API](https://platform.openai.com/docs/api-reference/responses) format and DeepSeek's Chat Completions API, enabling Codex (and other OpenAI Responses API clients) to use DeepSeek V4 models.

```
Codex (Claude Code) ──→ localhost:16889 ──→ https://api.deepseek.com/chat/completions
```

### Endpoints

| Endpoint | Description |
|----------|-------------|
| `POST /v1/responses` | Translate Responses API requests to Chat Completions, then translate responses back |
| `POST /v1/responses/compact` | Not supported — returns 501 |

### Environment Variables

| Env Var | Default | Description |
|---------|---------|-------------|
| `CODEX_DEFAULT_MODEL` | `deepseek-v4-pro` | Default model for Codex requests |
| `CODEX_MODEL_MAP` | `{}` | JSON map of client model names to DeepSeek model names (e.g., `{"claude-sonnet-4-6": "deepseek-v4-pro"}`) |
| `CODEX_UPSTREAM` | `https://api.deepseek.com/chat/completions` | DeepSeek Chat Completions API URL |

### Usage

Point Codex to the same proxy URL:

```json
"OPENAI_BASE_URL": "http://localhost:16889"
```

The proxy auto-detects Responses API requests (`/v1/responses`) and applies the appropriate translation. All existing Anthropic API proxy features remain unchanged.
```

**更新现有环境变量表格:**
在现有的 Configuration 表格下增加标注:
> "For Codex usage, see the [Codex Support](#codex-support) section above."

**添加 coverage badge 引用:**
在顶部的 badge 行增加:
```
[![Coverage](https://img.shields.io/badge/coverage-85%25-brightgreen)](coverage.svg)
```
或者由 CI 生成的 badges 服务 URL。使用静态 reference（先写占位 85%, CI 生成后自动更新 SVG）。

**中文 README.zh-CN.md 同步修改:**
在 "## 为什么需要这个代理" 后插入 "## Codex 支持" 章节, 内容翻译英文版并保持一致:

```markdown
## Codex 支持

dsv4-cc-proxy 也支持 [OpenAI Responses API](https://platform.openai.com/docs/api-reference/responses) 格式与 DeepSeek Chat Completions API 之间的协议翻译, 让 Codex（及其他 OpenAI Responses API 客户端）能使用 DeepSeek V4 模型。

### 端点

| 端点 | 说明 |
|------|------|
| `POST /v1/responses` | Responses API 请求翻译为 Chat Completions, 再将响应翻译回 Responses 格式 |
| `POST /v1/responses/compact` | 暂不支持（返回 501）|

### 环境变量

| 环境变量 | 默认值 | 说明 |
|---------|--------|------|
| `CODEX_DEFAULT_MODEL` | `deepseek-v4-pro` | 响应的默认模型名 |
| `CODEX_MODEL_MAP` | `{}` | 客户端模型名到 DeepSeek 模型名的 JSON 映射（如 `{"claude-sonnet-4-6": "deepseek-v4-pro"}`） |
| `CODEX_UPSTREAM` | `https://api.deepseek.com/chat/completions` | DeepSeek Chat Completions API 地址 |

### 使用方法

将 Codex 指向同一个代理地址:

(同上英文版用法)

代理会自动识别 Responses API 请求并执行相应的翻译。所有现有的 Anthropic API 代理功能不变。
```

**约束:**
- 不删除或修改现有章节内容, 仅追加
- 中文版与英文版内容同步, 中文术语前后一致
- 若 README 中有项目结构图（`## Project Structure`）, 需更新以包含 codex/ 子包
- 更新 "## Testing" 节, 将其改为 `pytest tests/ -v`
</action>

<verify>
<automated>
grep -c "## Codex Support" README.md && grep -c "## Codex 支持" README.zh-CN.md && grep -c "CODEX_DEFAULT_MODEL" README.md && grep -c "CODEX_UPSTREAM" README.md
</automated>
</verify>

<acceptance_criteria>
- `grep "## Codex Support" README.md` 返回 1 行
- `grep "## Codex 支持" README.zh-CN.md` 返回 1 行
- `grep "CODEX_DEFAULT_MODEL" README.md` 返回 1 行
- `grep "CODEX_UPSTREAM" README.md` 返回 1 行
- `grep "POST /v1/responses" README.md` 返回 2+ 行（endpoints 表和描述中）
- 中文版对应翻译一致
</acceptance_criteria>
<done>README 中英文版 Codex Support 章节添加完成</done>
</task>

<task type="auto">
<name>Task 2: Update CHANGELOG.md + Create docs/dev/codex-integration.md</name>
<files>CHANGELOG.md, docs/dev/codex-integration.md</files>

<read_first>
- CHANGELOG.md (完整阅读, 了解现有格式和 [1.8.0] 条目的结构)
- docs/dev/deepseek-thinking-proxy.md (作为 codex-integration.md 的格式模板)
</read_first>

<action>
**Part A: CHANGELOG.md 更新**

在文件顶部 `# Changelog` 后, 现有 `## [1.8.1]` 前插入 `## [2.0.0]` 条目。使用现有格式, 日期为 2026-06-07:

```markdown
## [2.0.0] - 2026-06-07

### Added
- Codex 子包（config.py / translate.py / tools.py / sse.py）— OpenAI Responses API 协议翻译
- proxy Codex handler（/v1/responses 和 /v1/responses/compact 路由）
- E2E 集成测试（tests/test_e2e.py）
- __main__.py CLI 测试（tests/test_main.py）
- 技术文档 docs/dev/codex-integration.md
- CI 覆盖率徽章生成
- PyPI OIDC Trusted Publishing 发布
- Docker semver sha 标签

### Changed
- 版本升至 2.0.0（Codex 双协议支持 — MAJOR 版本）
- README / README.zh-CN.md 增加 Codex 支持章节
- CI 全面运行 `pytest tests/ -v`（全量测试）
- CI 中 PyPI 发布从 API token 切换为 OIDC
- 测试重构消除冗余，改善组织
```

**Part B: docs/dev/codex-integration.md 创建**

创建技术深度文档, 模仿 docs/dev/deepseek-thinking-proxy.md 的 YAML frontmatter + 章节结构。

```markdown
---
title: Codex Integration
type: tool
category: infrastructure
tags: [deepseek, proxy, codex, responses-api, sse, streaming]
created: 2026-06-07
updated: 2026-06-07
---

# Codex Integration

[完整文档, 包含以下章节:]

## 概述
- 解决的问题: Codex CLI 使用 DeepSeek V4 时的协议不兼容
- 三层处理: 请求翻译 + 工具转换 + SSE 流转换

## 架构
ASCII 架构图展示:
```
Codex (Claude Code) ──→ localhost:16889 ──→ https://api.deepseek.com/chat/completions
                          │
                          │ (三层处理)
                          │
                          ←── Responses API JSON/SSE
                          │
            ┌─────────────┼─────────────┐
            │             │             │
            ▼             ▼             ▼
       translate.py    tools.py       sse.py
       (请求翻译)      (工具转换)     (SSE 状态机)
```

## 模块依赖图
展示 codex/ 子包内模块的关系:
- config.py <-- translate.py <-- proxy.py (responses_handler)
- tools.py <-- translate.py
- sse.py <-- proxy.py (_handle_stream_response)

## 翻译流程: 请求
Responses API input -> Chat Completions messages 的翻译规则:
- instructions 合并到 system message
- input 数组元素按 role 映射
- function_call / function_call_output 处理
- reasoning.effort -> thinking 参数

## 翻译流程: 流式响应
Chat SSE -> Responses API SSE 的完整翻译过程:
- delta.content -> response.output_text.delta
- delta.reasoning_content -> response.reasoning_text.delta
- delta.tool_calls -> response.function_call_arguments.delta
- 类型转换: reasoning -> text -> tool_calls 时 output_item.done/add 事件

## SSE 生命周期事件序列
完整的 SSE 事件顺序:
1. response.created
2. response.in_progress
3. response.output_item.added (message)
4. response.output_text.delta
5. response.output_item.done
6. response.completed

## 环境变量参考
列出 CODEX_DEFAULT_MODEL / CODEX_MODEL_MAP / CODEX_UPSTREAM 的值和用途

## 测试
- 单元测试位置和运行命令
- E2E 测试场景说明

## 版本历史
| 版本 | 变更 |
|------|------|
| v2.0.0 | 首次实现 — Codex 双协议支持 |
```
</action>

<verify>
<automated>
head -10 CHANGELOG.md | grep -c "## \[2.0.0\]" && head -20 docs/dev/codex-integration.md | grep -c "Codex" && head -10 docs/dev/codex-integration.md | grep -c "title:"
</automated>
</verify>

<acceptance_criteria>
- `head -10 CHANGELOG.md | grep "## \\[2.0.0\\]"` 返回 1 行
- docs/dev/codex-integration.md 存在且包含 YAML frontmatter + 5+ 个章节 heading
- docs/dev/codex-integration.md 包含 ASCII 架构图
- docs/dev/codex-integration.md 包含 SSE 生命周期事件序列
</acceptance_criteria>
<done>CHANGELOG 和 codex-integration.md 文档更新完成</done>
</task>

<task type="auto">
<name>Task 3: Update .github/workflows/ci.yml — CI 改进</name>
<files>.github/workflows/ci.yml</files>

<read_first>
- .github/workflows/ci.yml (完整阅读现有 CI 配置, 理解 test/lint/pages/publish 四个 job 的结构和依赖关系)
</read_first>

<action>
修改 .github/workflows/ci.yml, 执行以下四项变更:

**变更 1: 简化 test job 到单 Python 版本（D-16）**
```yaml
    # 删除:
    # strategy:
    #   matrix:
    #     python-version: ["3.11", "3.12", "3.13"]
    #   fail-fast: false
    # 改为:
    #   硬编码 python-version: "3.12"

    # 删除 matrix 后的 steps 中 ${{ matrix.python-version }} 引用, 改为 "3.12"
```

**变更 2: 全量测试运行 + 覆盖率报告 + 覆盖率 badge 生成（D-06, D-15）**
在 test job 的 "Run tests" step 中, 将命令改为:
```yaml
    - name: Run tests
      run: python -m pytest tests/ -v --cov=dsv4_cc_proxy --cov-report=term
```

添加覆盖率 badge 生成 step（在 Run tests 之后, 仅在 main 分支）:
```yaml
    - name: Install coverage-badge
      if: github.ref == 'refs/heads/main'
      run: pip install coverage-badge

    - name: Generate coverage badge
      if: github.ref == 'refs/heads/main'
      run: coverage-badge -o coverage.svg

    - name: Commit coverage badge
      if: github.ref == 'refs/heads/main'
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add coverage.svg
        git diff --staged --quiet || git commit -m "chore: update coverage badge [skip ci]"
        git push
```
注意: 如果 `coverage-badge` 命令在 CI 环境中不可用或输出格式不同, 备选方案使用 shields.io 动态 badge URL 嵌入 README。

**变更 3: PyPI OIDC Trusted Publishing（D-14）**

在 publish job 中:
```yaml
    permissions:
      contents: write
      id-token: write   # <-- 新增: OIDC 需要
```

移除 `password` 参数（不再需要 API token）:
```yaml
    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        print-hash: true
      # 删除了 password: ${{ secrets.PYPI_API_TOKEN }}
```

**变更 4: Docker 标签追加 sha（D-17）**

在 Docker metadata action 的 tags 列表中追加:
```yaml
          type=sha
```

**最终 publish job 结构验证:**
- permissions 包含 `id-token: write`
- PyPI 发布 step 无 `password:` 参数
- Docker metadata tags 包含 `type=sha`
- 现有 Docker semver 标签保持不变
- GitHub Release 创建步骤保持不变
- Post-publish verification 步骤保持不变
- Homebrew tap 更新步骤已移除（per CONTEXT.md Deferred Ideas — 待后续迭代）

**约束:**
- 不删除或修改现有 lint、pages 或 publish job 的其他部分
- 确保 coverage-badge 在 test job 中安装, 不污染 lint job
- commit back step 使用 `[skip ci]` 避免触发无限 CI 循环
- changes 需要通过 `diff` 验证
</action>

<verify>
<automated>
grep -c "id-token: write" .github/workflows/ci.yml && grep -c "pytest tests/ -v" .github/workflows/ci.yml && grep -c "type=sha" .github/workflows/ci.yml
</automated>
</verify>

<acceptance_criteria>
- `grep "id-token: write" .github/workflows/ci.yml` 返回 1 行
- `grep "pytest tests/ -v" .github/workflows/ci.yml` 返回 1 行
- `grep "type=sha" .github/workflows/ci.yml` 返回 1 行
- `grep "coverage-badge" .github/workflows/ci.yml` 返回 2+ 行（install + generate）
- `grep "password:" .github/workflows/ci.yml | grep -c "PYPI_API_TOKEN"` 返回 0（已移除）
- python-version matrix 简化为单版本
</acceptance_criteria>
<done>CI 配置更新完成: PyPI OIDC + coverage badge + 单版本 + Docker sha 标签</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> GitHub Secrets | CI 发布 job 使用 GitHub Secrets 中的 Docker 凭证, 需确保最小权限 |
| CI runner -> PyPI | PyPI OIDC 使用 id-token: write, 无需长期 Secret |
| CI runner -> git | coverage badge commit-back 使用 GITHUB_TOKEN, 需 `[skip ci]` 避免循环 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | CI publish job logs | mitigate | PyPI OIDC 不涉及 API token, 无 Secret 泄露风险。Docker credentials 使用 GitHub Secrets, CI 自动脱敏 |
| T-06-07 | Tampering | coverage badge commit | mitigate | badge commit 使用 `[skip ci]`, 只在 main 分支触发, 不启动其他 CI 流程 |
| T-06-08 | Denial of Service | CI infinite loop | mitigate | coverage badge 的 commit message 包含 `[skip ci]`, 避免 CI 触发 CI 的循环 |
</threat_model>

<verification>
1. `grep "## Codex Support" README.md && grep "## Codex 支持" README.zh-CN.md` -- README 章节存在
2. `head -10 CHANGELOG.md | grep "## \[2.0.0\]"` -- CHANGELOG 条目存在
3. `head -20 docs/dev/codex-integration.md | grep "title:"` -- 技术文档存在
4. `.github/workflows/ci.yml` 四项变更均通过 grep 验证
</verification>

<success_criteria>
- 4 个文档文件更新/创建完成
- CI 配置文件更新完成, 四项变更验证通过
- 所有文件均已 git add + commit
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-release/06-03-SUMMARY.md`
</output>
