---
phase: 03-tool-support
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_tools.py
  - dsv4_cc_proxy/codex/__init__.py
  - dsv4_cc_proxy/codex/translate.py
autonomous: true
requirements:
  - CODX-07
  - CODX-10

must_haves:
  truths:
    - "convert_tools is importable from dsv4_cc_proxy.codex"
    - "translate_request() automatically calls convert_tools() on body.tools when present"
    - "Flat tool format conversion is covered by tests with 5+ scenarios"
    - "Schema field stripping is covered by tests with 8+ field removals verified"
    - "Recursive cleaning through $defs/anyOf/properties/items is covered by tests"
    - "Edge cases: empty tools, null tools, missing parameters, unknown type, invalid schema are all tested"
    - "translate_request integrates tools conversion: result body has tools converted to Chat format"
    - "All 22 existing proxy tests still pass — zero regressions"

  artifacts:
    - path: "tests/test_tools.py"
      provides: "Comprehensive tool conversion + schema repair test suite"
      min_lines: 200
    - path: "dsv4_cc_proxy/codex/__init__.py"
      provides: "Subpackage export including convert_tools"
      contains_regex: "convert_tools"
    - path: "dsv4_cc_proxy/codex/translate.py"
      provides: "Integration: translate_request calls convert_tools on tools field"
      contains_regex: "convert_tools"

  key_links:
    - from: "dsv4_cc_proxy/codex/translate.py"
      to: "dsv4_cc_proxy/codex/tools.py"
      via: "import from dsv4_cc_proxy.codex.tools import convert_tools"
      pattern: "from dsv4_cc_proxy.codex.tools import convert_tools"
    - from: "dsv4_cc_proxy/codex/translate.py"
      to: "dsv4_cc_proxy/codex/tools.py"
      via: "call inside translate_request body: body['tools'] = convert_tools(body['tools'])"
      pattern: "convert_tools\\(body"
    - from: "dsv4_cc_proxy/codex/__init__.py"
      to: "dsv4_cc_proxy/codex/tools.py"
      via: "from dsv4_cc_proxy.codex.tools import convert_tools"
      pattern: "from dsv4_cc_proxy.codex.tools import convert_tools"
---

<objective>
Plan 03-02: 测试工具转换模块 + 集成到现有代码

Purpose: 为 Plan 03-01 创建的 tools.py 编写全面测试套件，并将其集成到现有 codex 子包中（__init__.py 导出，translate_request 调用）。确保 CODX-07 和 CODX-10 的所有行为都有测试覆盖，且不影响现有功能。

Output:
- tests/test_tools.py — 全面测试套件（~250+ 行，10+ 测试函数）
- dsv4_cc_proxy/codex/__init__.py — 更新导出 convert_tools
- dsv4_cc_proxy/codex/translate.py — 集成 convert_tools 调用到 translate_request
</objective>

<execution_context>
@/Users/lihaoxuan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lihaoxuan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions — MUST implement exactly
@.planning/phases/03-tool-support/03-CONTEXT.md

# Exact code patterns with analog files
@.planning/phases/03-tool-support/03-PATTERNS.md

# Existing analogs
@dsv4_cc_proxy/codex/__init__.py
@dsv4_cc_proxy/codex/translate.py
@dsv4_cc_proxy/codex/tools.py
@tests/test_translate.py

<interfaces>
From dsv4_cc_proxy/codex/tools.py (the module under test):
```python
def convert_tools(tools: list[dict]) -> list[dict]:
    # Returns transformed tool list, never mutates input
    pass
```

From dsv4_cc_proxy/codex/translate.py (the caller that needs integration):
```python
def translate_request(request_body: dict) -> dict:
    body = copy.deepcopy(request_body)
    # ... existing translation steps 1-7 ...
    # step 8: body.pop("include", None) — line 281
    # step 9: body["messages"] = messages — line 284
    # Integration point: insert convert_tools call between step 8 and step 9
```

From dsv4_cc_proxy/codex/__init__.py (the exporter):
```python
# dsv4-cc-proxy / codex — Codex CLI 协议翻译子包

from dsv4_cc_proxy.codex.config import resolve_model
from dsv4_cc_proxy.codex.translate import translate_request

__all__ = ["resolve_model", "translate_request"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_tools.py with comprehensive test suite</name>

  <files>tests/test_tools.py</files>

  <read_first>
    /Users/lihaoxuan/code/P14_dsv4ToCC/.planning/phases/03-tool-support/03-CONTEXT.md
    /Users/lihaoxuan/code/P14_dsv4ToCC/.planning/phases/03-tool-support/03-RESEARCH.md
    /Users/lihaoxuan/code/P14_dsv4ToCC/.planning/phases/03-tool-support/03-PATTERNS.md
    /Users/lihaoxuan/code/P14_dsv4ToCC/dsv4_cc_proxy/codex/tools.py
    /Users/lihaoxuan/code/P14_dsv4ToCC/tests/test_translate.py
  </read_first>

  <action>
    创建 tests/test_tools.py 文件。

    严格遵循 PATTERNS.md 中的测试模式（继承 test_translate.py 的 AAA 结构）。
    覆盖 D-13 指定的所有测试场景。以下 5 个测试组，精确组名和测试函数。

    **测试文件头部**（与 test_translate.py 一致）:

    ```python
    """dsv4-cc-proxy codex 工具转换单元测试。

    覆盖: 格式转换、schema 字段剥离、递归清理、边界情况。
    运行: python3 -m pytest tests/test_tools.py -v
    """
    ```

    **导入**（只导入 tools 模块本身，不需要 monkeypatch/reload）:
    ```python
    import copy

    import dsv4_cc_proxy.codex.tools as codex_tools
    ```

    **注意**: 与 test_translate.py 不同，tools.py 不依赖环境变量，因此不需要 `monkeypatch` 或 `reload`。

    ---

    ### 组 1: 格式转换（CODX-07）

    分隔注释: `# ======== 组 1: 格式转换 ========`

    **test_convert_flat_to_nested()**
    - 输入: 单个扁平工具 `{"type": "function", "name": "get_weather", "description": "X", "parameters": {...}}`
    - 验证: `result[0]["type"] == "function"`
    - 验证: `"function" in result[0]`
    - 验证: `result[0]["function"]["name"] == "get_weather"`
    - 验证: `result[0]["function"]["description"] == "X"`
    - 验证: `"parameters" in result[0]["function"]`
    - 验证: 扁平字段已移除 `"name" not in result[0]`
    - 验证: 扁平字段已移除 `"description" not in result[0]`

    **test_convert_already_nested_pass_through()**
    - 输入: 已嵌套工具 `{"type": "function", "function": {"name": "get_weather", "description": "X"}}`
    - 验证: 结果不变（无双重嵌套）`result[0] == input[0]`
    - 验证: `"function" in result[0]`
    - 验证: `result[0]["function"]["name"] == "get_weather"`

    **test_convert_empty_tools_list()**
    - 输入: `[]`
    - 验证: `result == []`

    **test_convert_none_tools()**
    - 输入: `None`
    - 验证: `result == []`

    **test_convert_multiple_tools()**
    - 输入: 包含 3 个工具的列表（2 个扁平 + 1 个已嵌套）
    - 验证: 每个工具正确转换
    - 验证: 已嵌套工具保持原样

    **test_convert_tools_immutable()**
    - 输入: 一个扁平工具的列表
    - 使用 `copy.deepcopy` 保存原始输入
    - 调用 `convert_tools(tools)`
    - 验证: `tools == tools_copy`（输入未被修改）

    ---

    ### 组 2: Schema 字段剥离（CODX-10）

    分隔注释: `# ======== 组 2: Schema 字段剥离 ========`

    **test_clean_schema_strips_all_unsupported()**
    - 输入: 包含所有 8 个不兼容字段的 schema
    ```python
    params = {
        "type": "string",
        "default": "fallback",
        "readOnly": True,
        "writeOnly": True,
        "examples": ["ex1", "ex2"],
        "minLength": 0,
        "maxLength": 100,
        "minItems": 1,
        "maxItems": 10,
    }
    ```
    - 验证: 8 个字段都不在结果中
    - 验证: 合法字段 `"type"` 保留

    **test_clean_schema_removes_empty_enum()**
    - 输入: `{"type": "string", "enum": []}`
    - 验证: `"enum" not in result`

    **test_clean_schema_preserves_non_empty_enum()**
    - 输入: `{"type": "string", "enum": ["a", "b"]}`
    - 验证: `result["enum"] == ["a", "b"]`

    **test_clean_schema_keeps_valid_fields()**
    - 输入: `{"type": "object", "properties": {"x": {"type": "string"}}, "required": ["x"]}`
    - 验证: `"type"` 保留, `"properties"` 保留, `"required"` 保留

    ---

    ### 组 3: 递归清理（CODX-10, D-06）

    分隔注释: `# ======== 组 3: 递归清理 ========`

    **test_recursive_via_properties()**
    - 输入: 嵌套 3 层的 schema（properties > properties > default）
    ```python
    params = {
        "type": "object",
        "properties": {
            "addr": {
                "type": "object",
                "properties": {
                    "city": {"type": "string", "default": "NYC"}
                }
            }
        }
    }
    ```
    - 验证: 内部 `"default"` 被剥离 — 递归进入 nested properties

    **test_recursive_via_defs()**
    - 输入: 带 $defs 的 schema，defs 中有 default
    ```python
    params = {
        "type": "object",
        "$defs": {
            "Address": {
                "type": "object",
                "properties": {"city": {"type": "string", "default": "NYC"}}
            }
        }
    }
    ```
    - 验证: $defs 中的 default 被剥离

    **test_recursive_via_anyof()**
    - 输入: 带 anyOf 的 schema，分支中有 default
    ```python
    params = {
        "type": "object",
        "anyOf": [
            {"type": "string", "default": "fallback"},
            {"type": "integer", "default": 0},
        ]
    }
    ```
    - 验证: anyOf 各分支中的 default 被剥离

    **test_recursive_via_items()**
    - 输入: 数组类型 schema，items 中有 default
    ```python
    params = {
        "type": "array",
        "items": {"type": "string", "default": "item_default"}
    }
    ```
    - 验证: items 中的 default 被剥离

    ---

    ### 组 4: 边界情况

    分隔注释: `# ======== 组 4: 边界情况 ========`

    **test_unknown_type_passes_through()**
    - 输入: `{"type": "custom_type", "data": "test"}`
    - 验证: 结果与原输入相同
    - 验证: 不产生双重包装

    **test_missing_parameters_skips_schema_repair()**
    - 输入: 扁平工具但无 parameters 字段
    - 验证: 格式正确转换（有 function 键，有 name）
    - 验证: 无 parameters 在 function 内（不崩溃，跳过 schema repair）

    **test_tool_with_strict_field()**
    - 输入: `{"type": "function", "name": "fn", "strict": True, "parameters": {"type": "object"}}`
    - 验证: strict 被移到 function 内 `result[0]["function"]["strict"] == True`
    - 验证: 顶层无 strict `"strict" not in result[0]`

    **test_unknown_tool_type_preserved()**
    - 输入: `{"type": "function", "name": "fn", "parameters": {"type": "object"}}` + `{"type": "unknown_type_named_xyz"}`
    - 验证: function 工具正常转换，未知类型原样通过

    ---

    ### 组 5: 错误处理（D-07）

    分隔注释: `# ======== 组 5: 错误处理 ========`

    **test_invalid_parameters_type_raises_valueerror()**
    - 需要: `import pytest`
    - 输入: parameters 为字符串
    ```python
    tools = [{"type": "function", "name": "bad", "parameters": "invalid_string"}]
    ```
    - 验证: `pytest.raises(ValueError, match="Invalid tool parameters")`
    - 同上测试 parameters 为整数、None 时（None 应跳过不报错）

    **test_invalid_parameters_none_skipped()**
    - 输入: parameters 为 None
    - 验证: `result[0]["function"]["parameters"] is None`（不报错，D-09 跳过）

    **test_non_dict_tool_skipped()**
    - 输入: 列表中含有非 dict 元素
    - 验证: 不抛出异常，非 dict 元素被跳过
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_tools.py -v -x 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    以下所有条件通过 grep 和 pytest 验证:

    1. tests/test_tools.py 文件存在且至少 200 行
    2. 文件包含至少 18 个 test_ 函数（根据上述场景计数）
    3. test_convert_flat_to_nested 测试扁平→嵌套转换
    4. test_convert_already_nested_pass_through 测试已嵌套通过
    5. test_convert_empty_tools_list 和 test_convert_none_tools 测试空输入
    6. test_convert_multiple_tools 测试多工具列表
    7. test_convert_tools_immutable 测试不可变性
    8. test_clean_schema_strips_all_unsupported 测试 8 个字段剥离
    9. test_clean_schema_removes_empty_enum 测试空 enum 删除
    10. test_clean_schema_preserves_non_empty_enum 测试非空 enum 保留
    11. test_recursive_via_properties / defs / anyof / items 测试递归路径（4 个函数）
    12. test_unknown_type_passes_through 测试未知类型
    13. test_missing_parameters_skips_schema_repair 测试缺少 parameters
    14. test_tool_with_strict_field 测试 strict 字段处理
    15. test_invalid_parameters_type_raises_valueerror 测试无效参数异常
    16. test_non_dict_tool_skipped 测试非 dict 工具跳过
    17. `python3 -m pytest tests/test_tools.py -v -x` 全部通过（绿色）
  </acceptance_criteria>

  <done>
    test_tools.py 文件创建完毕，覆盖 CODX-07 和 CODX-10 的全部预期行为。
    所有 5 组测试（格式转换、字段剥离、递归清理、边界、错误处理）实现。
    pytest tests/test_tools.py -v -x 通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: Update __init__.py to export convert_tools</name>

  <files>dsv4_cc_proxy/codex/__init__.py</files>

  <read_first>
    /Users/lihaoxuan/code/P14_dsv4ToCC/dsv4_cc_proxy/codex/__init__.py
  </read_first>

  <action>
    更新 dsv4_cc_proxy/codex/__init__.py，添加 convert_tools 为第三个导出函数。
    严格遵循 PATTERNS.md 的精确修改。

    当前内容:
    ```python
    # dsv4-cc-proxy / codex — Codex CLI 协议翻译子包

    from dsv4_cc_proxy.codex.config import resolve_model
    from dsv4_cc_proxy.codex.translate import translate_request

    __all__ = ["resolve_model", "translate_request"]
    ```

    修改后的内容（D-03）:
    ```python
    # dsv4-cc-proxy / codex — Codex CLI 协议翻译子包

    from dsv4_cc_proxy.codex.config import resolve_model
    from dsv4_cc_proxy.codex.tools import convert_tools
    from dsv4_cc_proxy.codex.translate import translate_request

    __all__ = ["convert_tools", "resolve_model", "translate_request"]
    ```

    保持:
    - import 顺序按模块名字母序: config → tools → translate
    - __all__ 列表按名称字母序: convert_tools → resolve_model → translate_request
    - 模块注释行不变
    - 每行一个 import
  </action>

  <verify>
    <automated>python3 -c "from dsv4_cc_proxy.codex import convert_tools, resolve_model, translate_request; print('All three exports OK')"</automated>
  </verify>

  <acceptance_criteria>
    1. __init__.py 包含 `from dsv4_cc_proxy.codex.tools import convert_tools`
    2. __init__.py 的 __all__ 包含 `"convert_tools"`
    3. __all__ 中按字母序排列: convert_tools, resolve_model, translate_request
    4. import 顺序: config → tools → translate（模块名字母序）
    5. `python3 -c "from dsv4_cc_proxy.codex import convert_tools"` 成功
    6. `python3 -c "from dsv4_cc_proxy.codex import resolve_model, translate_request"` 仍然成功
  </acceptance_criteria>

  <done>
    convert_tools 可从子包直接导入，与 resolve_model、translate_request 导出风格一致。
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate convert_tools into translate_request</name>

  <files>dsv4_cc_proxy/codex/translate.py</files>

  <read_first>
    /Users/lihaoxuan/code/P14_dsv4ToCC/dsv4_cc_proxy/codex/translate.py
    /Users/lihaoxuan/code/P14_dsv4ToCC/.planning/phases/03-tool-support/03-CONTEXT.md
  </read_first>

  <action>
    在 translate.py 中做两处精确修改（D-02 集成位置）。

    ### 修改 1: 添加 import（模块顶部，现有 import 之间）

    在 `from dsv4_cc_proxy.codex.config import CODEX_DEFAULT_MODEL, CODEX_UPSTREAM, resolve_model` 之后添加:

    ```python
    from dsv4_cc_proxy.codex.tools import convert_tools
    ```

    import 顺序当前为:
    ```
    from __future__ import annotations

    import copy
    import json
    import logging

    from dsv4_cc_proxy.codex.config import CODEX_DEFAULT_MODEL, CODEX_UPSTREAM, resolve_model
    # ← convert_tools import 插在这里
    ```

    ### 修改 2: 在 translate_request() 中添加 convert_tools 调用

    当前 `translate_request()` 步骤 8 和步骤 9 之间（line 281 和 line 284 之间）:

    ```python
        # 8. 移除 Responses-only 字段
        body.pop("include", None)

        # ← 工具定义格式转换插在这里

        # 9. 设置 messages
        body["messages"] = messages
    ```

    插入的代码（D-02 精确位置、RESEARCH.md 建议）:

    ```python
        # 9. 工具定义格式转换 (Phase 3, CODX-07, CODX-10)
        if "tools" in body:
            body["tools"] = convert_tools(body["tools"])
    ```

    插入后步骤 9 变为步骤 10:
    ```python
        # 8. 移除 Responses-only 字段
        body.pop("include", None)

        # 9. 工具定义格式转换 (Phase 3, CODX-07, CODX-10)
        if "tools" in body:
            body["tools"] = convert_tools(body["tools"])

        # 10. 设置 messages
        body["messages"] = messages
    ```

    关键约束:
    - `if "tools" in body` — 只有 body 中有 tools 字段才调用（与 D-02 一致）
    - 调用位置在 `body.pop("include", None)` 之后 — 因为 tools 转换不依赖 include
    - 调用位置在 `body["messages"] = messages` 之前 — 因为 tools 和 messages 在此阶段独立
    - 用 convert_tools 的返回结果直接替换 body["tools"]
  </action>

  <verify>
    <automated>python3 -c "
from dsv4_cc_proxy.codex.translate import translate_request
# 验证集成: 有 tools 字段时应当转换
result = translate_request({
    'model': 'test',
    'input': [{'role': 'user', 'content': 'Hello'}],
    'tools': [{'type': 'function', 'name': 'test', 'description': 'desc', 'parameters': {'type': 'object'}}]
})
assert 'tools' in result
assert 'function' in result['tools'][0]
assert result['tools'][0]['function']['name'] == 'test'
# 验证扁平字段已移除
assert 'name' not in result['tools'][0]
print('translate_request + convert_tools integration OK')
"